describe('group', () => {
  it('should', () => {
    expect(2).toBeTruthy();
  });
});

describe('group', () => {
  const t = 'hashem';
  t = 'naser';
  it('should', () => {
    expect(0).toBeFalsy();
  });
});
